{
  "name": "prerendercloud-nodejs",
  "version": "0.9.0",
  "description": "Express middleware for prerendering javascript-rendered pages with https://www.prerender.cloud/ for SEO and open graph tags",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf distribution",
    "build": "npm run clean && babel source --presets babel-preset-es2015 --out-dir distribution",
    "test": "jasmine",
    "prepublish": "npm run build"
  },
  "author": "Jonathan Otto <support@prerender.cloud> (https://www.prerender.cloud/)",
  "license": "MIT",
  "dependencies": {
    "debug": "^2.2.0",
    "express": "^4.14.0",
    "request": "^2.74.0"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "jasmine": "2.4.0",
    "nock": "^8.0.0"
  }
}
